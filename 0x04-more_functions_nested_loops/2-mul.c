#include <main.h>

/**
 * mul - multiplies int a and int b
 * @a: int to be multiplied with b
 * @b: int to be multiplied with a
 * Return: always 0
 */
int mul(int a, int b)
{
	return(int a * int b);
}
