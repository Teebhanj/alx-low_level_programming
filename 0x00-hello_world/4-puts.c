#include <stdio.h>

/**
 * main- prints "programming is like builing a multilingual puzzle
 * Return: 0 if exited as espected, else non-zero
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
