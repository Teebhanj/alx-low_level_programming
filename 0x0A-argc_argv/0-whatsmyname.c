#include <stdio.h>

/*
 * main - prints the name of program, followed by a new line.
 * @argc: argument count.
 * @argc: array that contian the program line argument.
 * Return: return 0.
 */

int main (int argc, char *argv[])
{
	printf("my name is %s\n", argv[0])
		return 0;
}
