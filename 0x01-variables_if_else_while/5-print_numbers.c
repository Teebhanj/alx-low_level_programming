#include <stdio.h>
#include <stdlib.h>
/**
 * main - Holds source code/
 * Description: Prints single digit decimals
 * Return: returns 0
 */
int main(void)
{
	char a;

	for (a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	putchar('\n');
	return (0);
}
